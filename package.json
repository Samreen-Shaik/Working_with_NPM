{
  "scripts": {
    "test": "jest",
    "format": "prettier --write .",
    "lint": "prettier --check .",
    "prepare": "husky install",
    "precommit": "npm run lint && npm test"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "jest": "^29.6.2",
    "prettier": "^3.0.0"
  }
}
